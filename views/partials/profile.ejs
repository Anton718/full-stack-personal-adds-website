
<h1>PROFILE</h1>
<div class="card" style="width: 18rem">
  <div class="card-body">
    <div>
      <div  style="border: 1px solid black; border-radius: 5px; margin-bottom: 2px;">
            <img  class="card-img-top"
            <%try {%>
              <% if (!image) {%>
              src="images/profile-icon-9.png" alt="user photo"
              <% } else {%>
              src="<%=image%>" alt="user photo"
                <%}%>
                <%} catch {null;}%>
            >
      </div>
      <form action="/addimage" method="POST" enctype="multipart/form-data">
        <input type="file" name="image" id="image" style="width: 200px; margin-bottom: 2px;" >
        <input type="submit" value="Upload">
      </form>
    </div>
    <p>
      <h4 class="card-title">Name: <%=user%></h4>
    </p>
    <p class="card-text">About: <%=yourBio%></p>
      <p>
        <form action="/addbio" method="POST">
          <textarea name="bio"  cols="20" rows="2" placeholder="About yourself"></textarea>
          <input type="submit" value="Update" />
        </form>
      </p>
    <a href="/private" class="btn btn-primary">Private messages</a>
  </div>
</div>
