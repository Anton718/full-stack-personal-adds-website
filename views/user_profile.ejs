<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="css/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>User Profile</title>
</head>
<body id="body">
    <%- include('./partials/navbar2.ejs') %>
    <% if (token) { %>
    <%- include('./partials/loginbar.ejs') %>
      <% } else { %>
    <%- include('./partials/logoutbar.ejs') %>
        <% } %>
    <div class="container">
        <% if (data[0]) {%>
        <h1>User Profile</h1>
        <div class="card" style="width: 18rem">
          <div class="card-body">
            <h5 class="card-title"><%=data[0].name%></h5>
            <p class="card-text"><%=data[0].bio%></p>
          </div>
        </div>
        <p>
            <form action="/sendprivate" method="POST">
                <div class="mb-3">
                    <input type="hidden" name="to_user"  value="<%= data[0].name %>">
                    <input type="hidden" name="bio"  value="<%= data[0].bio %>">
                    <label for="private" class="form-label">Send them a message:</label>
                    <input type="text" class="form-control" id="private" name="private">
                    <input style="margin-top: 5px;" type="submit" value="send">
                  </div>
            </form>
          </p>
        <%}%>
        <p>
            <% if (response) { %>
            <div class="alert alert-primary" role="alert">
                <%= response %>
            </div>
            <% } %>
        </p>
    </div>
</div>
</body>
</html>